# Django 프로젝트 필수 패키지
Django==5.2.7
asgiref==3.10.0
sqlparse==0.5.3

# 프로덕션 웹 서버
gunicorn==21.2.0

# PostgreSQL 연결 (Railway 배포 시 필요)
# 로컬 개발 시에는 선택사항 (SQLite 사용)
psycopg2-binary==2.9.9

# DATABASE_URL 파싱
dj-database-url==2.1.0

# 정적 파일 서빙
whitenoise==6.6.0
